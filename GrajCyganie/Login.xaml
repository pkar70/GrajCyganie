<Page
    x:Class="GrajCyganie.Login"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GrajCyganie"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Loaded="Page_Loaded">

    <StackPanel Margin="5,5,5,5" >
        <TextBlock FontSize="18" HorizontalAlignment="Center" Text="Login" Margin="0,0,0,15"/>
        <TextBox Header="Identyfikator" x:Name="uiUserName" PlaceholderText="(podaj swój identyfikator)" IsTextPredictionEnabled="False" />
        <Button HorizontalAlignment="Center" Margin="5,15,5,15" Content="Login" Click="uiOk_Click" />
        <TextBlock x:Name="uiLoginRes" />
    </StackPanel>
</Page>
