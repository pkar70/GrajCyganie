<Page
    x:Class="GrajCyganie.DbaseAndStorage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GrajCyganie"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Loaded="Page_Loaded">

    <Grid Margin="5,5,5,5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Skąd brać dane?" HorizontalAlignment="Center" FontSize="20" />
        <ComboBox Grid.Row="1" Name="uiBaza" Header="Baza danych:" />
        <TextBox Grid.Row="2" Name="uiLocalPath" Header="Local file system:" />
        <TextBox Grid.Row="3" Name="uiLocalODPath" Header="Local OneDrive shadow:" />
        <Button Grid.Row="4" Margin="0,10,0,0" Content="Save!" Click="uiSave_Click" HorizontalAlignment="Center" VerticalAlignment="Top" />
    </Grid>
</Page>
